<div class="posts">
    <ul class="list">
        <% page.posts.each(function(item, itemIndex){ %>
            <%- partial('_partial/item', {item: item}) %>
        <% }); %>
    </ul>
    <%- partial('_partial/pagination') %>
</div>
